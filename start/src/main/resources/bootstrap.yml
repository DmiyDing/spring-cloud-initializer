server:
  port: 10001
  servlet:
    context-path: /demo

spring:
  profiles:
    active: dev
  application:
    name: demo
  main:
    allow-bean-definition-overriding: true

# 使用okhttp作为feign请求工具，默认为HttpURLConnection
feign:
  httpclient:
    enabled: true
  okhttp:
    enabled: false

#mybatis-plus:
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

